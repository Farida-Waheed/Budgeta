name: budgeta_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.1.5+1
  flutter_bloc: ^9.1.1

  # Utilities
  uuid: ^4.5.2
  shared_preferences: ^2.5.3
  flutter_native_splash: ^2.4.7
  flutter_speed_dial: ^7.0.0

  # iOS icons
  cupertino_icons: ^1.0.8

  # ----------------------------------------
  # 📌 ICON PACKAGES
  # ----------------------------------------

  # ✔ Lucide Icons (clean, modern outlines)
  lucide_icons: ^0.257.0

  # ✔ Feather Icons (minimal, thin icons)
  flutter_feather_icons: ^2.0.0

  # ✔ Phosphor Icons (thin, light, bold… all styles)
  phosphor_flutter: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/app_logo.png
