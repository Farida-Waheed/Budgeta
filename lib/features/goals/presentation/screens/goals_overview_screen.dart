// lib/features/goals/presentation/screens/goals_overview_screen.dart
import 'package:flutter/material.dart';

class GoalsOverviewScreen extends StatelessWidget {
  const GoalsOverviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Goals Overview'),
      ),
      body: const Center(
        child: Text('Goals overview coming soon...'),
      ),
    );
  }
}
